# Architecture inputs (elements, interfaces, variants)

Elements: PSX-42 sense IC → analog front end → MCU MC-8S (application, diagnostics) → CAN transceiver → CAN bus.
Variants: single-channel (base), dual-channel (redundant sense IC, ASIL D variant).
